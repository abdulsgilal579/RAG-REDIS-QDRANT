# RAG-REDIS-QDRANT
A high-performance RAG pipeline that extracts text from PDFs, stores vector embeddings in Qdrant, and uses Redis as a semantic cache to reduce redundant LLM calls lowering latency and cost while using Groq for response generation.
